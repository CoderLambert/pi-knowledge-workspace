# P2-T09 — Retrieval benchmark runner

Status: **PARTIAL — REAL DEVELOPMENT REPORT GENERATED**

## Objective

Automate reproducible report generation for the retrieval metrics already defined across P2 without coupling report generation to a specific retriever or tuning loop.

## Corrected direct base

P2-T06 / `experiment/p2-dense-retrieval-adapter` / PR #38.

After real P2-T06 development evidence rejected both Dense profiles, canonical PR #41 was restacked directly onto #38. Historical P2-T07 sqlite-vec / #39 and P2-T08 Hybrid/RRF / #40 are intentionally bypassed because they are no longer required evidence for ADR-029.

## Implemented scope

`src/knowledge/eval/retrievalBenchmarkRunner.ts` remains a pure observation-driven runner:

- caller explicitly selects `development` or `holdout`;
- the Golden Dataset is filtered to exactly that split;
- every named variant carries a canonicalized configuration record;
- duplicate variant ids fail closed;
- each variant's observations are evaluated through Stable Evidence overlap metrics;
- observations from another split are rejected;
- output records Recall@10, MRR, all-required-Evidence coverage, category failure counts, latency and resource usage;
- deterministic Markdown rendering includes configuration, failures and no-answer diagnostics.

The runner does not execute retrieval, choose parameters, fetch models or invent missing observations.

## Real generated evidence

Support PR #52 executed the frozen FTS production retrieval path and fed all 50 development observations into this runner.

GitHub Actions run: `34329322920`

Evidence artifact: `10095148994`

Artifact digest: `sha256:079dc6b4a46c90136e274395d76ba63219e5c43e2ccb913135d5561364ca3672`

Runner environment:

```text
Ubuntu 24.04.4
Node 24.20.0
better-sqlite3 13.0.3
SQLite 3.53.4
FTS5 = true
```

Focused P2-T09/Golden Dataset/challenge/SearchQuery/FTS suite: **8 files / 29 tests PASS**.

Production evidence path:

```text
openKnowledgeDatabase
→ canonicalizeParsedArtifact
→ chunkParsedArtifact
→ Fts5BaselineIndex
→ IndexBuildPublisher
→ SearchQueryApi
→ runRetrievalBenchmark
→ renderRetrievalBenchmarkMarkdown
```

Frozen configuration:

```text
retriever = sqlite-fts5
tokenizer = unicode61
lexicalProfile = baseline
naturalLanguageCompiler = quoted-literal-or
topK = 10
corpusChunks = 38
challengeChunks = 35
```

Development metrics generated by the actual P2-T09 runner:

```text
queries: 50
answerable: 42
no-answer: 8
Recall@10: 1.0
MRR: 0.9365079365079365
all-required Evidence coverage: 1.0
category failures: none
no-answer queries with any hit: 8 / 8
median latency: 0.794373 ms
p95 latency: 1.334205 ms
max latency: 3.400616 ms
peak RSS: 105,443,328 bytes
FTS dbstat allocation: 49,152 bytes
```

The committed `eval/reports/retrieval-benchmark.md` is the generated Markdown from that run rather than a hand-authored numeric summary.

The artifact also preserves complete development observations plus provenance. Deterministic development-dataset hash recorded by the evidence runner:

`949cf28c36a3bfe6438e831aa96573ff10d30169f52dbc6b4192fca848fc40a3`

GitHub runner latency/RSS are runner-specific and are not Omarchy target-machine performance claims.

## Retrieval candidate interpretation

P2-T05 froze plain FTS as the least-complex lexical winner. P2-T06 then tested two fixed multilingual Dense profiles and both materially regressed Recall, MRR and all-required Evidence coverage.

Therefore the valid P2-T09 comparison set currently contains one evidence-backed retrieval candidate only: **FTS baseline**. Dense/Hybrid rows are intentionally absent rather than fabricated from rejected or unexecuted candidates.

## Holdout discipline

The P2-T09 development harness does not load holdout queries or holdout labels. The earlier P2-T05 one-shot holdout remains acceptance-only evidence for the frozen FTS profile; it was not used to generate or tune this development report.

## Gate classification

The evidence workflow succeeded and the focused runner/ancestry tests are green.

Repository-wide typecheck/lint/knip remain affected by inherited ancestry debt and must not be repaired inside this task merely for green CI.

P2-T09 itself has four style-only lint findings (`retrievalBenchmarkRunner.ts` plus its test). They remain task-attributable cleanup debt, so formal task status stays PARTIAL even though the required real benchmark evidence is now complete.

## Downstream

The generated retrieval evidence is sufficient to proceed to P2-T10 product-value baseline work without reopening sqlite-vec/Hybrid.

P3 remains prohibited until ADR-029 is Accepted.

## Out of scope

- executing or tuning new retrievers;
- embedding provider calls;
- sqlite-vec/Hybrid adoption;
- direct-file Pi execution;
- existing-product hands-on comparison;
- P3 answer generation.
