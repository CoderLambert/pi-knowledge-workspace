# Queries

Annotated retrieval questions live here as `GoldenQuery` records.

Every query has a stable id, text, `development` or `holdout` split, and one or more planned P2 categories. Holdout records are evaluation-only and must not be inspected to tune retrieval parameters.

A `no-answer` query intentionally has no Evidence label.