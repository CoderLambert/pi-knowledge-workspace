# Reports

Generated or manually reviewed retrieval-evaluation reports belong here.

Reports may contain aggregate metrics, per-category failures, latency/resource measurements and concrete failure cases. They are derived evidence and must not mutate the committed corpus/query/label ground truth.

Every report should identify the dataset revision, retrieval configuration and implementation revision it evaluated.